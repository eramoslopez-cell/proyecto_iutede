services:
  - type: web
    name: modeladosoftwarenoche
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd IUTEDE-main && uvicorn main:app --host 0.0.0.0 --port $PORT
  